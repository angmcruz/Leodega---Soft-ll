{
    "$schema": "https://railway.app/schemas/railway.schema.json",
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev && php artisan key:generate && php artisan optimize && php artisan migrate --force"
    },
    "deploy": {
        "startCommand": "php artisan serve --host 0.0.0.0 --port $PORT"
    }
}